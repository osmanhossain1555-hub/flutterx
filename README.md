# Finance Tracker PWA - Install Fix

This build improves install behavior on phones.

What changed:
- better install button fallback
- detects already-installed mode
- clearer message when browser does not expose the prompt
- same form editors and offline support
